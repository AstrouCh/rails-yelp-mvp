<h1>Add a new review to <%= @restaurant.name.capitalize %></h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
<%= link_to "⬅ Back to restaurants list", restaurants_path %>
